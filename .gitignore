node_modules/
dist/
.wrangler/
.dev.vars
.dev.vars.*
# The template is documentation, not a secret. (Git has no end-of-line comments.)
!.dev.vars.example
.env
.env.*
worker-configuration.d.ts
.DS_Store

# Your deployment's own config: worker name, KV namespace id, custom domain.
# Copy wrangler.example.jsonc to start one; it never needs committing.
wrangler.jsonc

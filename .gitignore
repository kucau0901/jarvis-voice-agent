node_modules/
dist/
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example   # the template is documentation, not a secret
.env
.env.*
worker-configuration.d.ts
.DS_Store

# Your deployment's own config: worker name, KV namespace id, custom domain.
# Copy wrangler.example.jsonc to start one; it never needs committing.
wrangler.jsonc
